# =================== 爬虫依赖（可选，按需安装） ===================
# 仅当需要启用论文爬虫 / 知网抓取时安装：pip install -r requirements-crawler.txt
# 轻量化服务器无需安装此项；未安装时相关爬虫模块自动降级（不会导入报错）。

# ---- 期刊/元数据爬虫 ----
arxiv>=2.0.0
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# ---- 知网（CNKI）浏览器自动化爬虫 ----
DrissionPage>=4.0.0

# ---- 验证码自动识别（#12 遗留项：并入主爬虫的 ddddocr 滑块识别）----
ddddocr>=1.4.7
Pillow>=10.0.0