# ================= 服务器端依赖（轻量化） =================
# git clone 后直接：pip install -r requirements.txt 即可运行 Web 服务 + AI。
# 不包含爬虫依赖；需要爬取数据时再：pip install -r requirements-crawler.txt
-r backend/requirements.txt