# PaperPulse 产品审视与重构方案

> 版本：v1.0 · 2026-08-25
> 定位：把 3,155 篇经管顶刊论文喂给 AI，帮研究者回答"研究什么、怎么验证、能不能发、发到哪"。

---

## 一、现状诊断：为什么"功能没发挥好"

**产品阶段判断**：这是一个"功能超市"（工具集），不是一个"有主线的产品"。80% 的工程量投在了**采集层**（109KB 验证码脚本、DrissionPage、6 个期刊爬虫、断点续爬），而**洞察层**（用户真正需要的"答案"）是薄的。这是功能没发挥好的根因。

### 五个核心问题

**1. 功能堆砌，没有主线（Kitchen-sink）**
7 个页面各自为政：论文列表、详情、作者、搜索、趋势、网络、系统管理。新用户打开首页不知道该先做什么。产品缺少一个 "job to be done"——它到底帮研究者完成什么任务？

**2. 重采集、轻洞察，AI 能力被浪费**
数据库里有 3,155 篇论文，但：

| 功能 | 使用痕迹（DB 实测） | 判断 |
|---|---|---|
| 单篇 AI 分析 | `paper_analyses` 仅 **4** 条 | 几乎没人用 |
| 追问对话 | `paper_chats` 仅 **0** 条 | 完全没人用 |
| AI 趋势报告 | `ai_analysis_reports` **13** 条 | 少量使用 |
| 选题库 | `topic_projects` **0** 条 | 新功能未落地 |

AI 是这产品最核心的差异化资产（GLM/Qwen/DeepSeek 三通道 + 降级 + 流式），但它被埋在"点进某篇论文 → 点分析按钮 → 等 3 分钟"的路径里，没有入口、没有批量、没有场景。

**3. 数据质量拖后腿，信任崩塌**
- 爬虫成功率低：`crawl_logs` 120 条里 **73 条失败**（经济研究、管理世界最近一次仍失败）
- **经济研究 / 经济学季刊 / 中国工业经济 三个期刊的论文摘要为空**（`fetchers.py:327/676/967`，注释明说"API 不返回摘要，需要单独获取"——从未实现）
- 占位 URL：`https://example.com/{journal}/{doi}`（`fetchers.py:221`）
- 两套评分公式并存：`scoring.py`（0.5/0.3/0.2）和 `crud.py:682`（0.35/0.35/0.30），同一篇论文分数取决于它走哪条爬虫路径

用户一旦点到一篇空摘要、假链接的论文，对整站信任就没了。**采集层的不稳定直接摧毁洞察层的价值。**

**4. 功能孤立，没有串联成工作流**
研究者的真实工作流是：**发现论文 → 读懂 → 找趋势/空白 → 形成选题 → 写综述/引用**。当前这些是孤岛：
- 论文分析结果不喂给选题验证
- 选题验证的 embedding 检索路径有**列错位 bug**（`topic.py:369-423`，embedding 路径把 title/abstract/keywords 全部错位读取，导致"近期"统计漏算、关键词重合度丢失）
- 收藏是 localStorage（`lib/cache.ts`），换设备即丢
- 没有个人阅读历史、标注、关注话题，产品没有"记忆"

**5. 没有用户体系，没有个人化**
全站 `user_id = "local"`（`models.py:183/201`）。一个"研究助手"产品，核心就是**个人上下文**：我在读什么、我关注什么方向、哪些论文我已经验证过。现在全站只有"数据视图"，没有"我的视图"。

### 值得保留的亮点
- **数据资产**：3,155 篇经管顶刊论文（2026 年 2,050 篇、2025 年 1,058 篇），关键词齐全（100%），相似度 3 万对已预计算
- **AI 工程化成熟**：双通道降级、模型在线配置、SSE 流式、结构化 JSON 输出，这些底子很好
- **选题中心雏形**（未提交的 Topic Lab）方向完全正确，和用户的真实需求（选题分析对话_2026-05-13.md 证明）高度吻合
- 前端工程干净：API 层统一、SSE 解析统一、无孤立组件

---

## 二、重新定位

> **从"论文发现与分析平台"→「经济管理研究者的选题智脑」**
>
> 一句话产品故事：**把 3,155 篇经管顶刊论文喂给 AI，帮研究者回答"研究什么、怎么验证、能不能发、发到哪"。**

**目标用户**：数量经济学/金融学方向的硕博生与青年研究者（以你自己为原型——从选题分析对话可知，你的真实痛点是"找到能发表的选题"）。

**核心价值主张（三层递进）**：
1. **看得全**——顶刊论文聚合（已有）
2. **看得懂**——AI 分析每篇论文、追问、相似推荐（已有但没入口）
3. **做得对**——趋势→空白→验证→期刊适配（**真正的主战场，差异化所在**）

**关键判断**：不要把资源继续堆在"更多期刊、更多爬虫"上。采集到 50 个期刊就够支撑洞察了。**把重心从"采"转向"用"。**

---

## 三、核心用户旅程（产品主线）

围绕研究者的一天重新组织导航，让每个页面回答一个明确问题：

| 步骤 | 用户问题 | 现有资产 | 缺口 |
|---|---|---|---|
| ① 发现 | 这个领域最近有什么值得读的？ | 首页列表/筛选/搜索 | 领域级"本周要读"推荐 |
| ② 读懂 | 这篇论文讲了什么、和我关心的问题什么关系？ | 详情页 AI 分析/追问/相似 | 入口太深、无批量分析 |
| ③ 洞察 | 这个领域的热点/空白是什么？ | 趋势页、网络图、gap 分析 | 图表不回答问题，缺"结论句" |
| ④ 选题 | 这个选题有没有人做过、我能做吗？ | Topic Lab（未提交） | 与论文库打通、补全验证闭环 |
| ⑤ 产出 | 怎么导出综述和引用、投哪个期刊？ | 无 | **整条链路缺失** |

---

## 四、功能规划（按优先级）

### P0 — 地基修复（先治病，再健体）

| # | 事项 | 具体内容 | 依据 |
|---|---|---|---|
| 1 | **统一评分公式** | 删掉 `scoring.py` 旧实现，以 `crud.py` 的 0.35/0.35/0.30 + 180 天半衰期为准，`ScoringSystem` 全部走同一入口 | `scoring.py` vs `crud.py:682` |
| 2 | **补齐空摘要** | 经济研究/经济学季刊/中国工业经济：为已有论文补抓详情页摘要（写一个 `backfill_abstracts` 任务，增量跑完再重算相似度） | `fetchers.py:327/676/967` |
| 3 | **修复选题验证列错位 bug** | `topic.py` embedding 路径的 SELECT 列序与 `_paper_brief` 对齐 | `topic.py:369-450` |
| 4 | **全量 embedding 补齐** | scheduler 路径让 `ai_processor.compute_embedding` 真正调用 provider embedding（现在恒返回 None，仅 backfill 接口补了 2,835/3,155） | `ai_processor.py:95` |
| 5 | **修复 `trending-topics` 时间窗** | `weeks_back` 参数启用 | `papers.py:141` |
| 6 | **关键单测** | scoring、similarity、gap 计算、topic brief 解析，各 3-5 个用例。目前全仓 **0 测试**，bug 反复靠人工重启发现 | 全仓无 test |

### P1 — 把已有功能"用起来"（产品体验重构）

**7. 研究工作台（Dashboard）——产品的主入口**
新建 `/dashboard`，替代现在"打开就是列表"的首页体验。一个页面回答三个问题：
- **今日值得读**：按评分 + 你关注子领域，推荐 5 篇（个性化：首次无关注则按热点）
- **领域快讯**：热点趋势 Top 5 + 一句话结论（由 AI 从 `topic_trends` 生成）
- **我的研究栈**：收藏、最近分析、最近验证的选题、进行中的选题

**8. AI 分析入口改造（把 4 条使用记录变成 400 条）**
- 列表页 PaperCard 直接加"AI 分析"快捷按钮（现在必须进详情页）
- **批量分析**：列表页多选 5-10 篇 → 一键生成"领域综述摘要"
- 详情页分析结果展示重构：现在只显示 4 段文本，改为**结构化卡片**（背景/方法/发现/意义）+ "与我的选题相关性"评分

**9. 搜索升级为"研究级检索"**
现在的搜索页比首页还弱（无评分/子领域/专题筛选，固定按日期排序，大量硬编码中文）。升级：全字段高级语法（`AND/OR/NOT`、`"引号短语"`、`author:xxx`）、与首页同套筛选器、结果按综合评分排序。

**10. 个人化（轻量用户体系）**
- 不用做注册登录，先用**本地身份**（`localStorage` 生成 userId）打通：收藏同步（后端表替代 localStorage）、阅读历史、已读/未读标记
- 关注子领域 → 驱动工作台推荐与**新论文提醒**

### P2 — 差异化主战场（选题智脑闭环）

**11. 补齐"⑤ 产出"环节——让选题闭环落地**
这是当前完全缺失、但最贴合你真实需求的一环：
- **综述生成**：输入一个选题 → AI 从论文库检索 20-30 篇 → 按"研究脉络/方法演进/争议点/空白"生成结构化文献综述（支持导出 Markdown/Word）
- **期刊适配建议**：给定选题 → 依据现有论文的期刊分布 + 期刊风格（可先内置 TOP20 期刊画像）→ 推荐 2-3 个投稿目标 + 理由
- **引用导出**：GB/T 7714 和 BibTeX 两种格式，详情页和列表页均可批量导出

**12. 选题中心升级为旗舰功能（把现有 Topic Lab 产品化）**
- **研究空白 → 选题**：gap 卡片加"转成选题"一键入口（现在是 `topic_projects` 空表）
- **验证闭环**：验证通过 → 自动生成一页"选题立项书"（含数据来源建议、可用数据、方法论，参考 `选题分析对话` 里的研究计划结构）
- **竞争地图**：验证时展示"这个话题谁在做、已发到哪里、近一年多少篇"（复用 network 数据）

**13. 图表从"展示"到"回答"**
- 网络图现在只是静态力导图，好看但无用。改为**查询驱动**：点一个关键词 → 动态生成该词的"研究版图"（共现词、增长、代表论文、期刊分布），一键转选题
- 趋势图每个话题点加"查看相关论文"和"AI 解读这段趋势"

### P3 — 扩展（后续）

- 数据源扩展：CSSCI、Crossref/OpenAlex API（学术全库检索，成本低）
- 多用户 + 团队协作（选题库共享）
- 新论文订阅推送（每日邮件/接口）

---

## 五、技术债务与工程建议

| 项 | 建议 |
|---|---|
| 品牌不一致 | 代码里 UI 显示 **ApplePaper**，README 叫 **PaperPulse**，package.json 也叫 paperpulse。统一成一个 |
| i18n 未完成 | 大量硬编码中文（Filters、author、search、network、topics、system 各页），英文用户看到中英混合界面；`<html lang>` 硬编码 zh |
| 作者页性能 | 每次打开下载 200 篇论文在客户端算统计，应加 `GET /authors/{name}/stats` 后端接口 |
| 系统管理页 | 1481 行单文件，四个 tab 混一个文件；模型配置 tab 还塞了端口/API key |
| 爬虫稳定性 | `_check_captcha` 已禁用（`fetchers_cnki_navi.py:119`），知网爬取依赖人工浏览器会话；建议把 `cnki_paper_captcha.py` 的 ddddocr 自动识别集成进主爬虫 |
| 主题切换 | D3 图随主题重绘丢失缩放位置；`@types/mermaid@9` 与运行时 v11 版本错位 |

---

## 六、里程碑建议

**M1（1-2 周）P0 地基**：修 bug、统一评分、补摘要/embedding、补 6 个单测。先让数据可信。
**M2（3-4 周）P1 用起来**：工作台 + AI 分析入口 + 搜索升级 + 个人化。让核心功能日活。
**M3（5-8 周）P2 差异化**：综述生成 + 期刊适配 + 选题中心产品化。形成完整闭环。
**M4（后续）P3**：数据源扩展、多用户、订阅。

---

## 一句话总结

这个项目的底子（数据 + AI 工程 + 爬虫）很好，问题是它"什么都是、但没回答用户最想要的问题"。**把产品主线从"看论文"改成"做研究决策"，把 AI 从"分析单篇论文"升级为"陪伴整个选题流程"，再补齐综述/引用/期刊适配这最后一公里**——你手里已有的功能就会自己转动起来。
